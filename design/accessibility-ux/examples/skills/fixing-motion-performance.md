# Example: fixing-motion-performance
Drawer/accordion/scroll remediation with `prefers-reduced-motion` fallback, transform-only transitions, and acceptance checks for stable focus and no unexpected scroll jumps.
