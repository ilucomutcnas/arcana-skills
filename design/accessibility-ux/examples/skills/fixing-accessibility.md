# Example: fixing-accessibility
Form+modal remediation with before/after code, keyboard path (`Tab→Submit→Error Summary→Field`), ARIA updates (`aria-describedby`, `aria-invalid`, labelled dialog), and acceptance checks for Escape close and focus return.
