# fixing-accessibility
Component workflows: dialogs (trap + return focus), menus (arrow key model), forms (label/error linkage), tabs/accordions (aria-controls/state sync), icon-only buttons (accessible name), live regions (announce state), focus recovery after async update.
Decision rules: prefer semantic controls, add ARIA only when semantics cannot express behavior, map each fix to WCAG criterion.
